# 小麥畫面漏項修正 Implementation Plan

> 依本輪已核可核對表，使用 executing-plans 逐項執行；不重新要求設計核可。

**Goal:** 完成播放行為、回饋分數、須知配分及任務標題漏項。
**Architecture:** 沿用 app.js 與 review.css；讀既有 API 的分數／組別，不更動資料庫與資料契約。
**Tech Stack:** 原生 JS/CSS、Node test、既有 Playwright/Chrome。
**Spec:** 主工作區 docs/testing/2026-09-26-xiaomai-gap-audit.md 及本輪使用者指示。

## Global Constraints
- 唯一資料庫 idealightsdg_cake；不得另建或切換。前端測試不連資料庫，攔截全部 API。
- 使用者已確認正式影片尚未完成；只修播放行為，保留待補素材。
- 不改既有回合進度、組別、作答，不改研究分組設計或問卷完成契約。
- 保留既有視覺；控制組後測前不顯示分數或解析。
- 既有全套測試含資料庫建立／刪除，不能在指定庫直接執行；如無法符合完整全測門檻，不合併／推送，明列限制。

## Review Focus
自動播放被阻擋、媒體載入失敗、重試後無法繼續、已看完影片重返、控制組提前看到得分、回饋分數未更新、手機標題區擠壓。

## Task 1 播放與觀看完成門檻
Files: assets/js/game/app.js、tests/ui-review.test.mjs、tests/ui-harness.mjs。
- [x] 不碰 DB 的既有測試建立基線。
- [x] 先測允許自動播放時影片實際開始、無控制列、播完前無下一步，看到失敗；最小實作後通過。
- [x] 再測自動播放拒絕時顯示開始播放，使用者點擊後可播完前進；紅→綠。
- [x] 再測媒體失敗可重載，重載後仍遵守完成門檻；紅→綠。
- [x] 觀看進度保存失敗提供重試，不重播已完成片段；沿用既有契約驗證。

## Task 2 回饋分數與文字
Files: assets/js/game/app.js、assets/css/game/review.css、tests/ui-review.test.mjs。
- [x] 先測實驗組最新本關／累積得分位於回饋標題區，滾動仍可見；紅→綠。
- [x] 驗證控制組無分數／教學小標，維持完成訊息。
- [x] 靜態文案直接修改並人工核對：每關六分／總分三十六分；推理評語獨立；回饋指定小標；本關任務主標。

## Task 3 驗收與交付
- [x] 桌機 1280×800、手機 375×812 兩輪截圖與八項自評，補正一次。
- [x] 學生／施測者 QA、語法、差異及可安全執行的完整前端測試。
- [x] 獨立程式審查；修正明確缺陷，更新 HANDOFF 與測試證據。
- [x] 根據全套測試與資料保護限制決定收尾；不以局部測試充當全套，不帶阻塞硬合併推送。

## 執行紀錄
- Ruling: 本輪範圍只修核對表明列畫面漏項；真實 AI 品質、外部問卷完成回傳及舊回合轉新版為限制，不擅改契約。
- Ruling: 不新增純文字斷言；測試集中在播放門檻、恢復與分數揭露行為。
- Pre-flight: Task 1/2 共用 app.js，但循序執行；測試共用前端 fixture，全部 API 被攔截，不可寫入真實資料。

- Task 1/2 complete: 逐項紅綠與既有契約回歸成功；Task 3 驗證完成，合併／推送依全測限制保留。完整證據見 docs/testing/2026-09-26-ui-followup.md。
